# emoji-mart declara peerDependency react@"^16 || ^17 || ^18", mas funciona em
# React 19. Sem isso npm install quebra em CI (Railway, GitHub Actions, etc).
# Remover quando emoji-mart publicar versão compatível com React 19.
legacy-peer-deps=true
